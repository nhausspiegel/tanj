# Refuse to install under a Node that violates package.json "engines"
# (e.g. the Intel Homebrew Node 26 on this repo's Apple Silicon machines).
# The preinstall guard (scripts/check-node-arch.cjs) additionally catches
# wrong-CPU-arch installs that pass the version check.
engine-strict=true
